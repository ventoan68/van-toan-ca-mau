let siteData=null;const $=s=>document.querySelector(s);const login=$('[data-login]'),dash=$('[data-dashboard]');
$('[data-toggle-password]').addEventListener('click',e=>{const i=e.currentTarget.parentElement.querySelector('input');i.type=i.type==='password'?'text':'password';e.currentTarget.textContent=i.type==='password'?'Hiện':'Ẩn'});
$('#login-form').addEventListener('submit',e=>{e.preventDefault();login.classList.add('hidden');dash.classList.remove('hidden');loadData();});
$('[data-logout]').addEventListener('click',()=>{dash.classList.add('hidden');login.classList.remove('hidden')});
document.querySelectorAll('[data-tab]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));$('#'+b.dataset.tab).classList.add('active');$('[data-title]').textContent=b.textContent;}));
async function loadData(){const r=await fetch('data/site.json');siteData=await r.json();renderAdmin();}
function renderAdmin(){ $('[data-count-products]').textContent=siteData.products.length;$('[data-count-projects]').textContent=siteData.projects.length;$('[data-count-posts]').textContent=siteData.posts.length;renderList('[data-list-products]',siteData.products,'category');renderList('[data-list-projects]',siteData.projects,'group');renderList('[data-list-posts]',siteData.posts,'date');fillSiteForm();}
function renderList(sel,items,meta){const tpl=$('#item-template');const wrap=$(sel);wrap.innerHTML='';items.forEach(item=>{const node=tpl.content.cloneNode(true);node.querySelector('img').src=item.cover||item.image;node.querySelector('img').alt=item.name||item.title;node.querySelector('b').textContent=item.name||item.title;node.querySelector('p').textContent=item.description;node.querySelector('small').textContent=item[meta]||'';node.querySelectorAll('button').forEach(btn=>btn.addEventListener('click',()=>alert('Chức năng đã chuẩn bị giao diện. Vui lòng cấu hình backend Cloudflare Pages Functions để lưu production an toàn.')));wrap.appendChild(node);});}
function getPath(obj,path){return path.split('.').reduce((a,k)=>a?.[k],obj)}function setPath(obj,path,val){const parts=path.split('.');let cur=obj;while(parts.length>1){cur=cur[parts.shift()]}cur[parts[0]]=val}
function fillSiteForm(){document.querySelectorAll('[data-site-form] [name]').forEach(i=>i.value=getPath(siteData,i.name)||'')}
$('[data-site-form]').addEventListener('submit',e=>{e.preventDefault();document.querySelectorAll('[data-site-form] [name]').forEach(i=>setPath(siteData,i.name,i.value));e.currentTarget.querySelector('.form-status').textContent='Đã cập nhật bản nháp trong bộ nhớ trình duyệt cho phiên xem thử. Production cần gọi endpoint /api/admin/site.'});
document.querySelectorAll('[data-add]').forEach(b=>b.addEventListener('click',()=>alert('Giao diện thêm/sửa/xóa, tải nhiều ảnh, chọn ảnh đại diện đã được chuẩn bị. Hãy cấu hình backend để lưu dữ liệu production.')));
